const Error = ({ error, addText }) => (
  <div>
    <p>oooooopppppsss it looks there is nothing to show</p>
    <p>this text from Error Component!</p>
    <h1>{addText}</h1>
    <h1>{error}</h1>
  </div>
);

export default Error;
